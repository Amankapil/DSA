#include <bits/stdc++.h>
using namespace std;

int main()
{
    // int maxx = ceil(5 / 2.0);
    // cout << maxx << endl;

    int r = 3;
    int c = 4;
    for (int i = 0; i < r; i++)
    {
        for (int j = 0; j < c; j++)
        {
            // cout << i << " " << j << endl;?

            //
            // int mid = arr[i][j];
        }
    }
}